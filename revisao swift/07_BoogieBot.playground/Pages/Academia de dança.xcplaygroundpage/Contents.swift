/*:
 ## Academia de dança
 
 Para preparar o BoogieBot para dançar, use esta linha de código:
*/
startBot()
/*:
 O BoogieBot pode fazer estes passos:
 - `leftArmUp()`, `leftArmDown()`, `rightArmUp()`, `rightArmDown()`
 - `leftLegUp()`, `leftLegDown()`, `rightLegUp()`, `rightLegDown()`
 - `shakeItLeft()`, `shakeItRight()`, `shakeItCenter()`
 - `jumpUp()`, `jumpDown()`
 - `fabulize()`, `defabulize()`
 
 Os passos estão bem claros, exceto `fabulize()` e `defabulize()`. O primeiro deixa o BoogieBot bem colorido, e o segundo faz com que ele volte a ser cinza-robô sem graça.
 
 É assim que você manda o BoogieBot se mexer:
 ```
(Exemplo):
fabulize()
shakeItLeft()
shakeItRight()
shakeItCenter()
```
 O BoogieBot para ao terminar a coreografia. Para ver os passos novamente, use Editor > Execute Playground (Editor > Executar playground). Se você mudar a coreografia, precisará começar tudo de novo.
*/
fabulize()
fabulize()
fabulize()
fabulize()
leftArmUp()
rightArmUp()

leftLegUp()
rightLegUp()
leftLegDown()
rightLegDown()
shakeItLeft()
shakeItRight()
shakeItCenter()
jumpUp()
jumpDown()

leftArmDown()
rightArmDown()
defabulize()

//: - experiment:\
//:(Experiência):\
//:Reorganize as linhas de código acima para remixar a coreografia. O BoogieBot executa os passos na ordem em que eles aparecem no playground.\
//:O que acontece se você usa as opções fabulize ou defabulize no meio de uma coreografia?
//:
//: 
//: De onde vêm essas funções de dança? Descubra na próxima página.
//:
//:[Anterior](@previous)  |  página 3 de 13  |  [Na sequência: APIs](@next)
